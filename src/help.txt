Project Manager version 1.0

commands:
	new	->	Creates a new project
	run	->	Runs current project
	build	->	Builds current project
	add	->      Adds a new langauge to the catalog
	list 	-> 	Lists all projects (make sure there is a all_projects.bpm file)
	find 	-> 	Finds a specified file.